# =============================================================================
# Zoom RTMS App - Environment Configuration
# =============================================================================

# Zoom App Credentials (get these from Zoom Marketplace - Contact Center App)
ZOOM_APP_CLIENT_ID=your_client_id_here
ZOOM_APP_CLIENT_SECRET=your_client_secret_here
ZOOM_SECRET_TOKEN=your_webhook_secret_token_here
ZOOM_BEARER_TOKEN=your_bearer_token_here

# Server Ports
PORT=3000                    # Frontend React dev server port
BACKEND_PORT=3001            # Backend API server port
RTMS_PORT=8080              # RTMS server port (internal)

# Public URLs (replace with your actual ngrok URL when testing with Zoom)
PUBLIC_URL=https://your-ngrok-url.ngrok-free.app
FRONTEND_URL=https://your-ngrok-url.ngrok-free.app
ZOOM_REDIRECT_URL=https://your-ngrok-url.ngrok-free.app/api/auth/

# Internal URLs (Docker - do not change)
FRONTEND_INTERNAL_URL=http://frontend:3000
RTMS_SERVER_URL=http://rtms:8080
BACKEND_URL=http://backend:3001

# React App Configuration
REACT_APP_BACKEND_URL=http://localhost:3001
REACT_APP_ZOOM_CLIENT_ID=your_client_id_here

# Security (generate with: openssl rand -hex 32 or node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=change_this_to_a_random_string_in_production

# Environment
NODE_ENV=development
ZOOM_HOST=https://zoom.us

# =============================================================================
# Quick Setup:
# 1. Copy this file to .env: cp .env.example .env
# 2. Add your Zoom credentials from Zoom Marketplace
# 3. Replace "your-ngrok-url" with your actual ngrok URL (run: ngrok http 3001)
# 4. Generate a SESSION_SECRET using one of the commands above
# 5. Run: docker-compose up
# =============================================================================
